@model ApplicationCore.Models.MovieCardModel

<div class="movie-card text-center">
    <img src="@Model.PosterUrl" alt="@Model.Title" class="img-fluid rounded shadow-sm mb-2" style="height: 300px; object-fit: cover;" />
</div>